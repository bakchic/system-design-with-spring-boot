server:
  port: 8082

spring:
  application:
    name: order

  datasource:
    url: jdbc:postgresql://postgres:5432/orders
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: kafka:9092